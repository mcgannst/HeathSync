# Copy to deploy/.env.pgadmin (gitignored).
# A superuser connection, used only by scripts/db.sh to create databases and roles and apply schema scripts.
PGADMIN_URL=postgresql://USER:PASSWORD@192.168.68.86:5432/postgres
